# Backend API URL (include /api). For local dev the default is http://localhost:8000/api.
# For Vercel: set this in Project Settings → Environment Variables as VITE_API_URL.
# VITE_API_URL=https://your-backend.example.com/api
